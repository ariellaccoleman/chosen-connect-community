
// Re-export specific formatters for backward compatibility
import { formatLocationWithDetails } from './formatters/locationFormatters';
import { formatAdminWithDetails } from './formatters/organizationFormatters';

export {
  formatLocationWithDetails,
  formatAdminWithDetails
};
